import { NavBar } from "../components/Navbar";
import { Router } from "../components/Router";

export const Layout = () => {
  return (
    <section>
      <NavBar />
      <Router />
    </section >
  );
};
